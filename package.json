{
  "name": "brainstorm",
  "version": "0.0.1",
  "description": "brainstorm",
  "productName": "brainstorm",
  "cordovaId": "org.cordova.quasar.app",
  "author": "wolfpack",
  "private": true,
  "scripts": {
    "build": "quasar build",
    "build-ios-pwa": "quasar build -m pwa -c",
    "build-mat-pwa": "quasar build -m pwa -c",
    "heroku-postbuild": "npm install --only=dev --no-shrinkwrap && npm run build",
    "lint": "eslint --ext .js,.vue src",
    "start": "node server.js",
    "test": "echo \"No test specified\" && exit 0",
    "postinstall": "npm install express"
  },
  "dependencies": {
    "bcrypt": "^3.0.0",
    "body-parser": "*",
    "connect-history-api-fallback": "^1.6.0",
    "express": "^4.17.1",
    "serve-static": "^1.14.1"
  },
  "devDependencies": {
    "@vue/eslint-config-standard": "^4.0.0",
    "babel-eslint": "^10.0.1",
    "eslint": "^5.10.0",
    "eslint-loader": "^2.1.1",
    "eslint-plugin-vue": "^5.0.0",
    "quasar-cli": "^0.17.0",
    "strip-ansi": "=3.0.1"
  },
  "engines": {
    "node": ">= 8.9.0",
    "npm": ">= 5.6.0",
    "yarn": ">= 1.6.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not ie <= 10"
  ],
  "resolutions": {
    "ajv": "6.8.1"
  }
}
